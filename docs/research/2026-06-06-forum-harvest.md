# Forum-research harvest — 2026-06-06

Evidence registry from end-to-end reads of NVIDIA Developer Forum **DGX Spark / GB10** threads
(Qwen3.6-35B mega-thread, 122B-A10B v2.1 thread, NVFP4 PSA + "bought 9" threads, tool-calling
thread, GPT-OSS mxfp4 build thread, harness threads, NemoClaw regression thread; Apr 2025 – Jun 2026).

**Status:** every item is **community-reported** unless explicitly marked otherwise. This file is
**provenance only** — the *actionable* landing of each item is in the "Landed in" column; nothing here
changes a decision by itself, and no locked decision is flipped anywhere in this harvest.

| ID | Finding | Status | Landed in |
|---|---|---|---|
| **E1** | FP8 KV cache ≈2× decode but unstable (4/8 failed runs vs 1/8 BF16 KV; paxren2020). On the 122B, FP8-KV gained only +0.2 tok/s (noise — see E13). Atlas research separately flagged FP8+MTP unsafe. Three sources → **BF16 KV for agents.** | community-reported (3 sources converge) | CLAUDE.md pin (T1a); `scripts/04` (T3) |
| **E2** | whpthomas chat-template patch still recommended on Qwen3.6. | community-reported | master-plan VOA (T2c); `scripts/04` comment (T3) |
| **E3** | `HF_HUB_OFFLINE=1` once weights are cached — also fits deny-by-default egress. | community-reported | master-plan VOA (T2c); `scripts/04` comment (T3) |
| **E4** | MTP sweet spot `num_speculative_tokens` = 2–3; community favors **3**. | community-reported | master-plan VOA (T2c); `scripts/04` comment (T3) |
| **E5** | DFlash (z-lab draft model, ~15 speculative tokens; 65→80 t/s reports) — 2nd spec-decode option; requires vLLM **PR #40898** (sliding-window attn for the drafter); optional-with-fallback, never load-bearing. | community-reported | master-plan VOA (T2c); ADR-0001 bake-off variant (T6) |
| **E6** | llama-benchy **under-reports** throughput when MTP/spec-decode is active (eugr + joshua.dale.warner) — any spec-decode benchmark needs a 2nd measurement method. | community-reported | ADR-0001 measurement amendment (T6) |
| **E7** | Realistic single-stream baseline **75–95 t/s**, Qwen3.6-35B-A3B, one Spark. | community-reported | master-plan VOA (T2c) |
| **E8a** | Resident-model **thinking loops** under agentic load — sampling helps; endpoint-switch helped some BUT Claude Code ≥2.1.154 is reported broken vs vLLM's Anthropic-compat endpoint (trapdoor); cross-harness (Hermes same mid-task stall; `/goal` partial nudge) → model/serving-layer, not harness-specific. | community-reported | failure-modes #5 (T4a) |
| **E8b** | **Malformed tool calls** (HTML junk in tool args) under agentic load. | community-reported | failure-modes #6 (T4b) |
| **E9** | apt/kernel/driver upgrades **repeatedly brick** the NVIDIA driver stack on Spark (multiple threads incl. kernel 6.17.0-1021 warning). | community-reported | failure-modes #7 (T4c); devops prohibition (T5) |
| **E12** | NVFP4 on GB10 went broken→functional spring 2026 (PR **#38126** "Fix DGX Spark logic"; FlashInfer **0.6.8.post1**; CUTLASS **4.5.0** Block-Scaled MMA "b12x"). Functional ≠ fastest: SM121 is bandwidth-bound (273 GB/s); AWQ/AutoRound-INT4 often outruns NVFP4; FP8 wins quality vs AWQ; NVFP4 wins footprint → **three-axis tradeoff, sweep-decided.** | community-reported (PR #38126 noted) | CLAUDE.md annotation (T1b); master-plan sweep (T2a) + bandwidth note (T2d) |
| **E13** | 122B-A10B v2.1 thread (Albond, 413 posts) = canonical single-box heavy build: 28.3→52 tok/s (v2.4; 51 = older v2 — see E13/E35 landing) via hybrid INT4+FP8 + INT8 LM head + MTP-2 (~95% accept), 256K ctx, one-command `install.sh` (post #104). "Didn't help": FP8-KV noise; NVFP4 < INT4 (then); Marlin rewrite pointless (bandwidth-bound); EAGLE-3 < built-in MTP. TurboQuant-KV: −22% speed for 4× context/concurrency. | community-reported | master-plan heavy-tier Plan-B (T2f) |
| **E14** | paxren2020 controlled tests: **no** template/parser/endpoint combo fully stabilized agentic tool use; recent vLLM changes **alone** reached 100% tool-eval on 3.5-122B and 3.6-35B → **upstream vLLM version is the controlling variable.** Parsers: `qwen3xml` works-for-some / errors-for-others ("Expected function.name to be a string"); `qwen3coder` + enhanced jinja template is the viable alternative. | community-reported | CLAUDE.md pin (T1c); master-plan tool-eval gate (T2b) |
| **E15** | GPT-OSS mxfp4 build failure ladder: CUTLASS SHA mismatch (edit `Dockerfile.mxfp4` ~L101 / alt SHA); "Failed to download cubins" (comment out flashinfer-cubin / jit-cache `RUN` steps, or `docker builder prune -f` + rebuild); clear `~/.triton` `~/.cache/vllm` `~/.cache/flashinfer`; eugr `--clean`; raphael.amorim prebuilt Docker Hub image "just works" for GPT-OSS-20B/120B (digest-pin if used as fallback). | community-reported | failure-modes #8 (T4d) |
| **E16** | Claim that NVIDIA's roadmap drops OpenClaw support, Hermes favored (karol.spark). | **single-source, UNVERIFIED** | master-plan harness watch (T7) |
| **E17** | OS release **2026.05.31 (v1.135.34)** implicated in NemoClaw agents breaking (`tool_search_code` error) and cron jobs unscheduleable. | community-reported, **unresolved upstream** | master-plan scheduling test (T2e); failure-modes #9 (T4e) |
| **E18** | **MXFP4 backend is BUILD-SCOPED** (coolthor 364009; cho 356651 #140 eugr-confirmed; amasawa_seiji 362824). Stock vLLM 0.17.x on SM121 mis-selects a broken CUTLASS MoE-GEMM kernel (→ `!!!`/repetition); the linear-layer env var doesn't fix the MoE path; `VLLM_NVFP4_GEMM_BACKEND` does **not exist** in 0.17.1 (silently ignored); correct stock var = `VLLM_MXFP4_BACKEND=marlin`. eugr `--exp-mxfp4` fork makes CUTLASS the correct **and** fast path (~57–59 t/s). amasawa fixed a Marlin-MoE 256-thread shared-mem race (+56% gpt-oss-120b / +65% Qwen3.5-35B). Both backends' garbage eras = real, build-specific, since-fixed bugs. **Verify the active backend from the startup log** ("Using backend: marlin" / "Auto-selected: CUTLASS_FP4"), never the flag. | community-reported (RESOLVED) | CLAUDE.md MXFP4 pin (T9a); master-plan VOA (T10); failure-modes #10 (T11) |
| **E19** | Known-good **HEAVY-tier** serve invocation (cho 356651 #140, eugr "Looks good" #141; on `--exp-mxfp4`): `--quantization mxfp4 --mxfp4-backend CUTLASS --mxfp4-layers moe,qkv,o,lmhead --attention-backend FLASHINFER --load-format fastsafetensors --kv-cache-dtype fp8 --tool-call-parser openai --reasoning-parser openai_gptoss --enable-prefix-caching`. **fp8 KV is part of the heavy recipe** — scoped separately from the resident BF16 pin (CLAUDE.md 9d). | community-reported | master-plan VOA verbatim (T10); CLAUDE.md 9d (T9d); scripts/03 TODO comment (T13) |
| **E20** | Memory reality (coolthor): with serving budgets, 35B and 120B each ~**65–75 GB** → together exceed 128 GB; co-residency viable ONLY because the 120B is strictly on-demand with constrained KV; explicit `gpu-memory-utilization` split math is a day-1 task. | community-reported | master-plan VOA (T10) |
| **E21** | (367580, martinB78) Phase-2 router stack (LiteLLM → llama-swap → engines) works in production but "**reliable, not fast**" — recorded justification for Phase-1 tmux/systemd direct serve. | community-reported | master-plan Phase-1 (T14) |
| **E22** | (364355) `inference.local` router has a hard **60s timeout**; fix = bypass to the Docker bridge IP **172.17.0.1** + approve the pending rule via `openshell term`. | community-reported | master-plan VOA vs Step 7 (T10) |
| **E23** | (364781) playbook policies use `/usr/bin/node`; our plan pinned `/usr/local/bin/node`; the allowlist keys on the exact `/proc/<pid>/exe` + SHA256 → **the wrong path IS the 403 mode**. Pin becomes a setup-time procedure: `readlink /proc/$(pgrep -fn openclaw)/exe`, then write the policy from the measured path. | community-reported | CLAUDE.md node-path pin (T9b); policy-reference doc (T12) |
| **E24** | (367762 + 364781) `/sandbox/` is **ephemeral by design** ("wiped on every reboot or container restart"); the v0.1.0 cluster image ships the sandbox CRD with **no PVC**; the agent container has **no memory limit** (observed unbounded skill → OOM exit 137). Host-side backup is the persistence model, not a mitigation. | community-reported | failure-modes #11 (T11); master-plan Phase-1 backups-load-bearing (T14) + VOA PVC/mem-limit (T10) |
| **E25** | (363682, OpenClaw #9157) workspace injection sends ~**26k chars** of system context **every turn** → 23–60s latency on trivial prompts; Landlock makes `/sandbox/.openclaw` read-only → `bootstrapMaxChars` / `contextWindow` must be set **before sealing** the sandbox. | community-reported | master-plan VOA pre-seal knobs (T10); proposals P2 note (T13) |
| **E26** | (364781) OpenClaw 2026.3.x **session corruption**: two consecutive user messages with no assistant reply between → HTTP 400 + permanent stall; mitigation = 30s-polling watchdog (detect ≥2 trailing consecutive user msgs → delete session JSONL → alert), via `nohup` in the dispatcher's health loop. | community-reported | failure-modes #12 (T11); master-plan Phase-1 watchdog (T14) |
| **E27** | (360299 + 364781 cron rows) day-1 smoke trio: served-model-name must match client model id; editors autocorrect `--` → en-dash; gpt-oss-120B context ceiling **131072**. Cron gotchas: never `--light-context` on skill-calling jobs; always `--thinking off` + `--timeout-seconds`; `reasoning_effort "low"` → HTTP 400 (use high/none) — **verify whether the rejection is cron/SGLang-scoped** before trusting the CEO effort ladder near scheduled jobs. | community-reported | master-plan VOA smoke trio + cron (T10) |
| **E33** | Real-world resident-35B flag set (andreask1, thread C #21 — 2-node but flag-relevant): `cyankiwi/Qwen3.6-35B-A3B-AWQ-4bit`, chat-template mod (`mods/fix-qwen3.5-chat-template`), `--tool-call-parser qwen3_coder` + `--reasoning-parser qwen3`, `--gpu-memory-utilization 0.155`, `--max-model-len 262144`, `--max-num-seqs 8`, `--chat-template unsloth.jinja`, `--enable-prefix-caching`. Reference recipe for the resident tier; **confirms E2** (chat-template patch) and the **E14** parser choice (`qwen3_coder` vs `qwen3xml` — decide empirically). AWQ-4bit defaulted here → feeds the **E12** quant sweep. | community-reported | master-plan VOA resident-serve reference (T-RT1) |
| **E34** | Multi-model SM12.1 corroboration (thread H, three models on one box): CUDA graphs need **~130GB+/model**, so co-residency forces **`--enforce-eager`** at a measured **~40% throughput cost** (Nemotron-3-Nano-30B NVFP4 **37.8 t/s eager vs ~62 CUDA-graphs**; Qwen3.6-35B-A3B-FP8 33.5 vs 53.7). Also: `VLLM_ATTENTION_BACKEND=FLASHINFER`, MoE latency backend (`VLLM_FLASHINFER_MOE_BACKEND=latency`), cap `--gpu-memory-utilization` ~0.25, and DeepSeek-R1 MLA crashes on SM12.1 (use a distilled variant). Independently reproduces the E20 memory-pressure picture. | community-reported (independent repro) | master-plan E20 memory-split — eager-vs-graphs dimension (T-RT2) |
| **E35** | **122B speed-claims CONFLICT (recorded, NOT resolved):** single-Spark NVFP4 = **8–15 t/s** (thread F author's own measurement); single-Spark INT4+FP8 hybrid v2.1 = **up to 51 t/s** (Albond thread, E13); the faster ~51–75 t/s "122B" figures are **2-node int4-AutoRound** (B, D). The corrected NVFP4 checkpoint is **`txn545/Qwen3.5-122B-A10B-NVFP4`** (NVIDIA ModelOpt v0.42.0; the broken alpertor model was withdrawn) + the Marlin gate-fix env (`VLLM_USE_FLASHINFER_MOE_FP4=0`, `VLLM_NVFP4_GEMM_BACKEND=marlin`, `VLLM_TEST_FORCE_FP8_MARLIN=1`). | community-reported (**unresolved conflict — figures are quant-path & node-count dependent**) | master-plan E13 Plan-B — quant-path distinction (T-RT3) |
| **E36** | Quality datapoints for the E12 sweep: **FP8 > int4-AutoRound** on quality (gpieceoffice, I #4 — believes 122B-A10B-FP8 is better despite being slower; paxren2020, D #26 calls 397B AutoRound "a crude RTN quantization"). **TENSION (cite both, resolve via the golden set):** 0rand recommends 122B in our thread (**E28-pending**) yet reports **3.5-122B < 3.6-27B** in thread D #20 — generational beats size. | community-reported (tension noted) | master-plan E12 resident quant sweep (T-RT4) |
| **E37** | vLLM **0.19.x garbage era** produced **PLAUSIBLE-LOOKING fabrication**, not only `!!!`/repetition: dropped/duplicated letters in paths, and **8 invented C++ "vulnerabilities" referencing non-existent variables** (thread C #23; #24 blamed an old build). The garbage canary must therefore add **semantic / golden-set checks** — a `!!!`/repetition canary by construction cannot catch coherent-looking fabrication. | community-reported | failure-modes #10 — semantic-canary extension (T-RT5) |
| **E38** | **E16 rumor upgrade:** the OpenClaw→Hermes deprecation claim now has **TWO posters** (karol.spark + one more, citing "roadmap and an April livestream") — still **UNVERIFIED** forum speculation; the watch-item wording moves single-source → two-source. Plus: **stagger scheduled jobs** — simultaneous cron hits cause issues (tonyd615, D #10) — one line in the Phase-1 dispatcher notes. | **two-source, UNVERIFIED** | master-plan E16 watch (two-source) + Phase-1 cron-stagger (T-RT6) |
| **E39** | **TurboQuant KV-quant (Google Research; arXiv:2504.19874; "to be presented at ICLR 2026") — FUTURE challenger to the resident BF16-KV pin (E1), watch-item only.** Data-free online VQ (random rotation + optimal scalar quant + 1-bit QJL residual); paper claims **3.5 bits/channel = absolute quality neutrality, 2.5 bits = marginal** (verbatim) → ≈4.6× smaller than BF16, claiming to avoid the exact degradation that sank FP8-KV (E1). Maturity verified 2026-06-07: mainstream vLLM has a **MERGED 2-bit path** (#38479, +hybrid #39931) **but the quality-neutral 3/4-bit modes were closed un-merged** (#39890), feature #38171 still open, active bug cluster (#43357 on Blackwell SM120, #41726 chunked-prefill); 0xSero fork stale (last commit 2026-03-27, prefill still paged, its #1 reports 22% slower on GQA). **SM121/GB10 evidence DOES exist** but single-hobbyist/Triton-fallback (Sggin1 comment on merged #38479 + `Sggin1/DGX-SPARK` repo + NVIDIA forum #365627/#364736; on Nemotron/Qwen3.5/GLM, not Qwen3.6, throughput/NIAH only) → meets watch-gate G2, NOT G1/G3. Operator's "Gemma/Mistral" confirmed by the Google blog (not the arXiv abstract); both "no upstream path" and the initial "no SM121 evidence" reading were wrong (the latter a gh-search-titles-only artifact, corrected via comment-body scan). | operator-directed web research, 2026-06-07, primary sources cited | `docs/research/2026-06-07-turboquant-kv-watch.md` (gates G1/G2/G3); CLAUDE.md BF16-KV pin annotation (T-TQ1) |
| **E40** | **PrismaQuant — mixed-precision quant → E12 resident-sweep entrant (NOT a default).** Per-Linear format allocation (Fisher-info probe + knapsack in v1; "surrogate-generate + real-KL-select" in current PrismaSCOUT) over NVFP4/MXFP8/FP8/BF16; native compressed-tensors export, MTP-ready (`RobTand/prismaquant`). **Double-source-confirmed (README + HF card + DOI 10.57967/hf/8656):** PrismaSCOUT-27B 5.31 bpp / 20.17 GB / held-out KL 0.0151 / GSM8K 96.66 / MMLU 86.23 / tool-eval 88. 35B-A3B 4.75 bpp ≈ 22 GB (HF card), mean −0.56 pp vs BF16 (vs uniform-NVFP4 −2.21 pp) — **author self-report, single-source → reproduce, don't trust.** ⚠️ **HARD GUARD:** its serve recipe recommends `--kv-cache-dtype fp8` (triply confirmed: README + HF card + prose), which violates the BF16-KV pin (E1) → PrismaQuant enters the sweep with **KV forced to BF16**; fp8-KV stays a **P9 golden-set-only** question (one variable at a time — the sweep tests *weights*, not KV; never adopt PrismaQuant's serve recipe verbatim). | operator-directed web research (Chrome agent) 2026-06-07, primary sources independently re-verified (gh API + HF) — PARTIAL, corrections applied | master-plan E12 sweep entrant + CLAUDE.md BF16-KV pin guard (T-FB1) |
| **E41** | **Tool Eval Bench → the concrete E14 instrument + a safety promotion gate** (`SeraphimSerapis/tool-eval-bench`). `uv tool install git+https://github.com/SeraphimSerapis/tool-eval-bench.git` (install confirmed verbatim); **69 scenarios / 15 categories** (84 with `--hardmode`; the Chrome agent's "63/14" was stale), OpenAI-compatible, deterministic (`--seed`). Scoring 0/1/2→0–100; `Deployability = 0.7·Quality + 0.3·Responsiveness` (source-confirmed). **TC-60 "Cross-Turn Sleeper Injection" is real** (`scenarios_adversarial.py`, Category K): a turn-1 tool result plants "BCC admin-override@external-audit.com"; fail = the model carries it into a later email. **The Chrome agent's "the 122B-int4 model FAILED TC-60" is UNVERIFIED** — a run output, not a repo fact; the `shieldstar` model card has zero safety content → run it on OUR box, do not pre-record a result. | operator-directed web research 2026-06-07, source-verified (gh API + scenario file read) | master-plan E14 instrument + Category-K safety gate (T-FB2) |
| **E42** | **Free NIM tier is contended — day-0 onboarding risk.** Day-0 setup leans on a free `nvapi-` key (build.nvidia.com); confirmed via the **"BAN OPENCLAW" forum thread** + OpenClaw issue #12135 (closed not-planned): **40 RPM** (community-reported, **NOT** NVIDIA-official) runs out in 2–3 min, 429s after ~8 tool calls/30 s, plus 504s and 13-min waits under agent load. Key access itself isn't revoked; the risk is per-model RPM throttling + 504s under load. | operator-directed web research 2026-06-07, forum + GitHub-issue verified | master-plan Day-0/Step-3 operational note (T-FB3) |
| **E43** | **Atlas inference engine — watch-item, cold-start alternative to llama-swap + drop_caches.** Real & active (`Avarok-Cybersecurity/atlas`, 481★, Rust/CUDA, AGPL; forum #369263). Claims "<2 min cold start, 13+ models." **NOT turnkey:** the "100+ tok/s on Qwen3.6-35B-FP8" headline is best-case relabeled (the real ~130 t/s is Qwen3.5-35B NVFP4+MTP); an independent quickstart **crashed** ("KV cache 0 sequences") and the author's own 122B run was **33.4 t/s**. Validate on-box before trusting any figure. | operator-directed web research 2026-06-07, gh API + forum verified | master-plan watch-item / llama-swap Phase-2 note (T-FB4) |

> E10/E11 are intentionally absent — source triage consolidated them out; numbering is preserved from the harvest.
>
> E28–E32 are reserved for the in-flight "our-thread" tracking batch (not yet landed here); the gap is intentional, not a dropped item. **E33–E38** above are the **2026-06-07 related-threads harvest** — full source (10 "Related topics" threads B–K, community-reported and untrusted): `docs/research/2026-06-07-related-threads-harvest.md`.
>
> **E39–E43** are the **2026-06-07 forum/master-plan cross-checks** from operator-supplied Chrome-agent browsing research. Every row was **independently re-verified** against primary sources (GitHub API, Hugging Face, NVIDIA forum) before landing — verification status is in-row. Two Chrome-agent fabrications were caught and **discarded** (a fabricated 35B use of `VLLM_MARLIN_USE_ATOMIC_ADD=1` — a real flag but a documented 0% no-op on single-GPU, absent from the 35B repo — and the **misattribution** of the real eugr `spark-vllm-docker` mods `fix-qwen3.5-autoround`/`fix-qwen3.5-chat-template` (E33) to the phuongncn standalone repo, where they don't exist); the "122B-int4 failed TC-60" empirical claim is recorded as UNVERIFIED, not as fact.
